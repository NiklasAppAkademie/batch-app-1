class User {
  String username;
  String avatarImagePath;
  bool agbAccepted;
  DateTime firstLogin;
  DateTime lastLogin;

  User(this.username, this.avatarImagePath, this.agbAccepted, this.firstLogin,
      this.lastLogin);
}
